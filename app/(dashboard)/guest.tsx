import { GuestDashboardContent } from '@/components/dashboard/GuestDashboardContent';

/** Stack entry for “My bookings” (guest trips); same data as Dashboard tab guest view. */
export default function GuestDashboardStackScreen() {
  return <GuestDashboardContent showAccountLinks />;
}
